package dev.dinokodes.chatsdk.adapter

import kotlinx.coroutines.CoroutineScope
import kotlinx.coroutines.MainScope

class AuthModuleAdapter internal constructor(
) : CoroutineScope by MainScope(){

//    fun validateNameSpace(namespace : String, callback: (result: Result?, error: Exception?) -> Unit) {
//        //wrap(callback) { onBoardModule?.validate(namespace) }
//    }
}